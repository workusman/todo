<%= form_for([@list, task]) do |f| %>
  <div class="" style="text-align: center">
    <div>
      <%= f.text_field :title, placeholder: "Task Title", style: "width: 50%" %>
    </div>
    <div class="m-3">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>